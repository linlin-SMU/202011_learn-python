# 直接创建列表
namelist = ['123', '345', '678']
for name in namelist:
    print(name)
# 在[]中插入数据，insert/append/
# 在[]中删除数据：delete/pop

# 内置函数： BTF (built-in function)--生成一个等差序列
# range(0,5) # range- 2开始， 11结束，2表示步长
# range(2, 11, 2)  2， 4， 6， 8， 10
list(range(0, 5))  # 转换为list

print(namelist[1:2])  # 得出来的为列表
